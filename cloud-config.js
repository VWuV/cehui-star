/* =====================================================================
 * 云端配置（当前版本不使用云端，无需修改本文件）
 *
 * 当前采用「本地保存 + 导出 CSV 交给老师」模式：
 * 学生数据自动保存在手机浏览器中，填完后点「导出全部数据」，
 * 把下载的 CSV 文件通过微信/QQ 发给老师即可。
 *
 * 本文件保留云端接口（enabled 保持 false 即关闭）。
 * 未来若接入云端（如腾讯云开发），再填写下面的密钥并改成 true。
 * ===================================================================== */
window.CLOUD_CONFIG = {
  enabled: false,                 // ← 保持 false（不使用云端）
  appId: "",                      // ← 预留：云端应用的 AppID
  appKey: "",                     // ← 预留：云端应用的 AppKey
  apiBase: "",                    // ← 预留：云端 API 地址
  teacherPassword: "123456"       // ← 预留：老师端查看密码
};
