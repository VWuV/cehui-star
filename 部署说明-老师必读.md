# 测绘之星 · 网页版发布与配置指南（老师必读）

这套网页包含：

- `index.html` —— 学生端（看视频、查指南、填表、提交作业）
- `teacher.html` —— 老师端（查看全班提交、一键导出全部数据）
- `cloud.js` + `vendor/spark-md5.min.js` —— 云端通用模块
- `cloud-config.js` —— 云端配置（老师填密钥用）
- `videos/` —— 教学视频
- `资料/` —— 原始表格与指南文档

---

## 第一步：注册 LeanCloud（数据云端，免费、不需要 VPN、不用备案域名）

学生提交的数据会存到 LeanCloud 国际版的云数据库。注册和配置步骤：

1. 打开 https://console.leancloud.app/register ，用邮箱注册（免费，无需手机号）；
2. 登录后点左上角「创建应用」，应用类型选**开发版**（免费）；
3. 进入应用后，点左下角「设置 → 应用凭证」，找到并复制三个值：
   - **AppID**
   - **AppKey**
   - **REST API 服务器地址**（形如 `https://xxxxxxxx.api.lncldglobal.com`）
4. 用记事本打开 `cloud-config.js`，把三个值分别填到 `appId`、`appKey`、`apiBase`；
5. 把 `enabled` 从 `false` 改成 `true`；
6. 把 `teacherPassword` 改成只有你知道的密码（老师端查看用）。

> 提示：LeanCloud 开发版免费额度足够几百名学生使用；密钥在网页文件里，学生理论上能看到，所以老师密码请设复杂一点。

---

## 第二步：把网页发布到 GitHub（免费）

1. 登录 https://github.com ，点右上角 **+ → New repository**；
2. Repository name 填 `cehui-star`，选择 **Public**，然后点 **Create repository**；
3. 在新页面里点 **uploading an existing file**；
4. 把下面这些**整个拖进网页**：`index.html`、`teacher.html`、`cloud.js`、`cloud-config.js`、`vendor` 文件夹、`videos` 文件夹、`资料` 文件夹；
5. 往下拉，点绿色 **Commit changes**；
6. 进入仓库 **Settings → Pages**，Source 选 **Deploy from a branch**，Branch 选 **main / (root)**，点 **Save**；
7. 等 1～2 分钟，网页会显示网址：`https://你的用户名.github.io/cehui-star/`。

> 如果单文件超过 25MB（网页上传限制），使用 git 命令行推送即可（本机已安装 git）。

---

## 第三步：学生和老师使用

- **学生**：手机打开网址（或扫二维码）→ 首页填好学号、姓名 → 看视频 → 填表 → 点「提交作业给老师」；
- **老师**：打开 `https://你的用户名.github.io/cehui-star/teacher.html` → 输入你设置的密码 → 查看/搜索/导出全部数据（导出的是 CSV，可用 WPS/Excel 打开）。

---

## 以后怎么更新内容

改完文件后，把新文件**重新拖拽上传覆盖**（文件名要一致），或使用 git 推送，等一两分钟网页就自动更新了，网址不变。

## 常见问题

- **学生点提交提示“云端尚未配置”**：说明 `cloud-config.js` 没填好或改完后没上传，检查 `enabled` 是否为 `true`、AppID/AppKey/apiBase 是否填对。
- **提交提示“云端地址未配置”**：`apiBase` 没填，按第一步第 3、4 点填写。
- **网页打不开**：确认仓库是 Public、Pages 已开启、`index.html` 在仓库根目录。
- **学生看不到视频**：确认 `videos` 文件夹已上传到仓库根目录。
